services:
  - type: static_site
    name: feedsense-frontend
    plan: free
    branch: main
    repo: https://github.com/anandrajkcet09/Feed-Sense-Frontend
    buildCommand: npm ci && npm run build
    publishPath: dist
    envVars:
      - key: VITE_API_URL
        scope: project
